# requirements_llm.txt
# Minimal dependencies for the LLM pipeline (math/chat/code) in this code bundle.
# Versions are aligned to the provided pip list where it matters.

# ---- core ----
torch==2.3.0+cu121
torchvision==0.18.0+cu121
torchaudio==2.3.0+cu121
numpy==1.26.4
pandas==2.2.2
tqdm==4.66.4

# ---- Hugging Face stack ----
transformers==4.40.2
datasets==2.19.1
accelerate==0.30.1
huggingface-hub==0.23.0
tokenizers==0.19.1
safetensors==0.4.3
sentencepiece==0.2.0

# ---- utilities ----
PyYAML==6.0.1
requests==2.31.0

# ---- logging (optional) ----
wandb==0.17.0

# ---- optional: code evaluation (HumanEval) ----
human-eval==1.0.3

# ---- optional: external judge scoring (if you use GPT-based evaluation) ----
openai==0.28.1

# ---- optional: tokenizer helpers / judge prompts ----
tiktoken==0.6.0
