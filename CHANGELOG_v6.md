# v6 update

This update aligns the supplemental tables to the current submitted paper numbering.

- Added `tables/main_table1_10seed_audit.csv` for main Table 1.
- Replaced the LLM summary with `tables/main_table2_llm_summary.csv` for main Table 2.
- Updated Flat-LoRA LLM values in the main Table 2 support files.
- Replaced stale LLM detail exports with clean paper-aligned summaries under `tables/llm/`.
- Renumbered appendix support files to match current Tables 3 through 18.
- Replaced the old v5 audit files with `table_coverage_audit_v6.csv` and `table_value_audit_v6.csv`.
